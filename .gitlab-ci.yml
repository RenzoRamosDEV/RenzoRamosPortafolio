image: node:18

cache:
  paths:
    - node_modules/

pages:
  stage: deploy
  script:
    - npm ci --legacy-peer-deps
    - npm run build
    - rm -rf public
    - mv dist public
  artifacts:
    paths:
      - public
  only:
    - main
